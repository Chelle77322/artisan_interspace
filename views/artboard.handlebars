<div class = "container">
    <div class = "card-body">
        <div class = "col-md-6">
            {{#each artisans as |artisan|}}
            {{>artisan-description artisan}}
            {{/each}}
        </div>
    </div>
</div>