<!DOCTYPE html>
<html lang="en">
<head>
 <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Buy My Art Work!</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href= "/css/style.css"/>   
</head>

  <body>
  <div class="wrapper">
      <header class="hero">
          <div>
            <nav class"nav">
                    <a class="link" href="/">Home</a>
                    <a class="link" href="./artboard">Artboard</a>
                    {{#if loggedIn}}
                    <a class="link" href="./logout" id="logout">Logout</a>
                    {{else}}
                    <a class="link" href="./login">Login</a>
                    {{/if}}
                    <a class = "link" href ="./signup">Artist Sign Up</a>
                </nav>          
          </div>
          </div>
      </header>

    <main class = "wtf">
      {{{ body }}}
    </main>
 
   {{#if loggedIn}}
    <script src="../../public/jss/logout.js"></script>
    {{/if}}
 
  
</body>


</html>