<!--Setting up the artisan handlebar to display each post-->
<div class = "text-center">
    <h2> {{name}}</h2>
    <p> {{{get_emoji}}}</p>
    <p>{{description}}</p>
    <p> Created by {{user.name}} on {{format_date date_created}}</p>

    <p>
        <span for ="img" aria-label = "money">💲</span>
        <span class = "dollar-amount"> {{format_amount needed_funding}}</span>Needed
    </p>
    <p>
        <button class = "btn btn-primary"> Buy this ArtWork</button>
    </p>
</div>